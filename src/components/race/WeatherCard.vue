<script setup lang="ts">
// WeatherData is not in MotoGP Pulselive API — component kept as placeholder
interface WeatherEntry {
  air_temperature: number
  track_temperature: number
  humidity: number
  wind_speed: number
}

defineProps<{ weather: WeatherEntry | null }>()
</script>

<template>
  <div v-if="weather" class="card p-5 grid grid-cols-2 md:grid-cols-4 gap-4">
    <div class="flex flex-col gap-1">
      <span class="text-gray-400 dark:text-gray-500 text-xs">دمای هوا</span>
      <span class="text-gray-900 dark:text-white font-bold tabular-nums text-lg">{{ weather.air_temperature }}°C</span>
    </div>
    <div class="flex flex-col gap-1">
      <span class="text-gray-400 dark:text-gray-500 text-xs">دمای آسفالت</span>
      <span class="text-gray-900 dark:text-white font-bold tabular-nums text-lg">{{ weather.track_temperature }}°C</span>
    </div>
    <div class="flex flex-col gap-1">
      <span class="text-gray-400 dark:text-gray-500 text-xs">رطوبت</span>
      <span class="text-gray-900 dark:text-white font-bold tabular-nums text-lg">{{ weather.humidity }}%</span>
    </div>
    <div class="flex flex-col gap-1">
      <span class="text-gray-400 dark:text-gray-500 text-xs">سرعت باد</span>
      <span class="text-gray-900 dark:text-white font-bold tabular-nums text-lg">{{ weather.wind_speed }} m/s</span>
    </div>
  </div>
</template>
